# Naomi Puyleart
# 11/7/25
# Program # 1: Student Pass/Fail
# Pick one of the short programs in one of the videos.
# Compile and run that code.

# Create class
class Student:
    def check_pass_fail(self):
        if self.marks >= 40:
            return True
        else:
            return False

    def __init__(self, name, marks):
        self.name = name
        self.marks = marks

# Create objects
student1 = Student('Harry', 85)
student2 = Student('Janet', 30)

did_pass =student1.check_pass_fail()
print(student1.name, student1.marks)
print(did_pass)
did_pass =student2.check_pass_fail()
print(student2.name, student2.marks)
print(did_pass)